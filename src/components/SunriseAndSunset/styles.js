import styled from "styled-components"
import { BigCard } from "../WeatherCurrent/styles"

export const SunriseAndSunsetContainer = styled(BigCard)`
    display: flex;
    flex-direction: column;
    align-items: center;

    p {
        font-weight: bold;
        margin-bottom: 1rem
    }
`
